<%= form_for(@projecttarget) do |f| %>
  <% if @projecttarget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projecttarget.errors.count, "error") %> prohibited this projecttarget from being saved:</h2>

      <ul>
      <% @projecttarget.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
